export interface Recipient {
  name: string;
  age: number;
  hobbies: string;
  occasion: string;
  priceRange: string;
  currency: string;
  deliveryTime: string;
}

export interface GiftSuggestion {
  name: string;
  brand: string;
  description: string;
  price: string;
  link: string;
}